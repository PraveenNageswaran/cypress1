def jsonArray = result.split("=").collect {
    it.contains(":") ? it : it.replaceAll(':', '=')
}

// Write JSON array to file
new File('output.json').withWriter { writer ->
    writer << new groovy.json.JsonBuilder(jsonArray).toPrettyString()
}
