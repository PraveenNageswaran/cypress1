// Replace '=' with ':' and remove trailing '}' for each key-value pair
def json = jsonString.replaceAll(/=/, ':').replaceAll(/([^{},]+)=([^{},]+)/, '"$1":"$2"')
println JsonOutput.prettyPrint(JsonOutput.toJson(json))
