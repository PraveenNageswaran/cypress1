def tableARows = vars.get("TableAResult").split("\n").toList()
def tableBRows = vars.get("TableBResult").split("\n").toList()

def matchedRows = tableARows.intersect(tableBRows)
def mismatchedRows = (tableARows - matchedRows) + (tableBRows - matchedRows)

log.info("Matched Rows: ${matchedRows}")
log.info("Mismatched Rows: ${mismatchedRows}")
