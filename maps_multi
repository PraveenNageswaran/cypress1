def isMap(obj) {
    return obj instanceof Map
}

def printMapKeys(map) {
    map.each { key, value ->
        if (isMap(value)) {
            printMapKeys(value)
        } else {
            println key
        }
    }
}

def parseAndPrintKeys(data) {
    if (isMap(data)) {
        data.each { key, value ->
            if (isMap(value)) {
                printMapKeys(value)
            } else {
                println key
            }
        }
    } else {
        println "value"
    }
}
