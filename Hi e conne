If you have a `krb5-01.ini` configuration file instead of the traditional `krb5.conf`, you can still use it in your script by specifying the path to this custom configuration file. Here's how you can modify the script to include the path to your `krb5-01.ini` config file:

```groovy
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.Statement;
import java.util.Properties;
import org.apache.hadoop.security.UserGroupInformation;

// Hive JDBC connection properties
def hiveUrl = "jdbc:hive2://your-hive-server:10000/default";
def principal = "your-service-principal@YOUR-REALM.COM";
def keytabLocation = "/path/to/your/keytab/file.keytab";
def coreSitePath = "/path/to/your/core-site.xml";
def hdfsSitePath = "/path/to/your/hdfs-site.xml";
def krb5IniPath = "/path/to/your/krb5-01.ini"; // Path to your krb5-01.ini config file
def hiveDriver = "org.apache.hive.jdbc.HiveDriver"; // Hive JDBC driver class

// Load the Hive JDBC driver
Class.forName(hiveDriver);

// Load CoreSite configuration
def coreSite = new File(coreSitePath).text;
def hdfsSite = new File(hdfsSitePath).text;

// Set Kerberos system properties
System.setProperty("java.security.krb5.conf", krb5IniPath);
System.setProperty("java.security.auth.login.config", "/etc/jaas.conf");

// Login with Kerberos keytab
UserGroupInformation.loginUserFromKeytab(principal, keytabLocation);

// Connect to Hive
Connection connection = DriverManager.getConnection(hiveUrl);

...

// Continue with the rest of your script for executing queries, processing results, and closing resources
```

In this updated script, I have added a `krb5IniPath` variable to hold the path to your `krb5-01.ini` configuration file. The script now sets the `java.security.krb5.conf` system property to point to your custom `krb5-01.ini` file.

Make sure to replace `"/path/to/your/krb5-01.ini"` with the actual path to your `krb5-01.ini` file in your system.

After making this modification, you should be able to use your custom `krb5-01.ini` configuration file in your script for setting up the Kerberos configuration. Let me know if you encounter any issues or have further questions!
